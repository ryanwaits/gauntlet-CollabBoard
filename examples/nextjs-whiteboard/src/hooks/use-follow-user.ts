"use client";

// Re-export the SDK hook — the full implementation now lives in @waits/openblocks-react.
// This file exists only for backward compatibility with existing imports.
export { useFollowUser } from "@waits/openblocks-react";
export type { UseFollowUserOptions, UseFollowUserReturn } from "@waits/openblocks-react";
